[project]
name = "ai-backend"
version = "0.1.0"
description = "Backend for ml-hub application"
readme = "README.md"
requires-python = ">=3.11.5"
dependencies = [
    "datasets>=4.8",
    "fastapi[standard]>=0.118.0",
    "huggingface-hub>=0.36.0",
    "jsonref>=1.1.0",
    "mlflow>=3.6",
    "pandas>=2.3",
    "retry==0.9.2",
]
