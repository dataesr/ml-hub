### Prerequisites The ai-core library requires the following tools to be installed and available in your system's $PATH:

Python 3.9+

ovhai CLI: Required for the ai_core.cloud.compute module. Follow the official OVH documentation for installation.