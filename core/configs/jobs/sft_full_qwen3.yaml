model_name: "Qwen/Qwen3-0.6B"
output_dir: "/output"
dataset_path: "/data/sft_train.jsonl"

use_lora: false

max_seq_length: 4096
num_train_epochs: 2
per_device_train_batch_size: 8
gradient_accumulation_steps: 2
learning_rate: 5.0e-5
bf16: true

mlflow:
  experiment: "sft-runs"
  run_name: "qwen3-full-run-01"
